# Form implementation generated from reading ui file 'Humidity_Window.ui'
#
# Created by: PySide6 UI code generator 6.4.2
#
# WARNING: Any manual changes made to this file will be lost when pyuic6 is
# run again.  Do not edit this file unless you know what you are doing.


from PySide6 import QtCore, QtGui, QtWidgets
import psycopg2
from config import config
from matplotlib.backends.backend_qt5agg import FigureCanvasQTAgg as FigureCanvas
from matplotlib.figure import Figure
import matplotlib.dates as mdates
from datetime import *
import pandas as pd
import os

basedir = r"\\erp-eipsa-datos\Comunes\EIPSA-ERP"


def imagen_to_base64(imagen):
    """
    Converts an image in PNG format to a base64 encoded string.

    Args:
        imagen: An instance of QImage or QPixmap to be converted.
    Return: 
        A base64 encoded string representing the image in PNG format.
    """
    buffer = QtCore.QBuffer()
    buffer.open(QtCore.QIODevice.OpenModeFlag.WriteOnly)
    imagen.save(buffer, ".png")
    base64_data = buffer.data().toBase64().data().decode()
    return base64_data

class AlignDelegate(QtWidgets.QStyledItemDelegate):
    """
    A custom item delegate for aligning cell content in a QTableView or QTableWidget to the center.

    Inherits from:
        QtWidgets.QStyledItemDelegate: Provides custom rendering and editing for table items.

    """
    def initStyleOption(self, option, index):
        """
        Initializes the style option for the item, setting its display alignment to center.

        Args:
            option (QtWidgets.QStyleOptionViewItem): The style option to initialize.
            index (QtCore.QModelIndex): The model index of the item.
        """
        super(AlignDelegate, self).initStyleOption(option, index)
        option.displayAlignment = QtCore.Qt.AlignmentFlag.AlignCenter

class ColorDelegate(QtWidgets.QItemDelegate):
    """
    A custom item delegate for applying background colors to cells in a QTableView or QTableWidget.

    Inherits from:
        QtWidgets.QItemDelegate: Provides custom rendering for table items.
    """
    def __init__(self, parent=None):
        """
        Initializes the ColorDelegate, setting up the color mapping from the database.

        Args:
            parent (QtWidgets.QWidget, optional): The parent widget. Defaults to None.
        """
        super().__init__(parent)

    def paint(self, painter, option, index: QtCore.QModelIndex):
        """
        Paints the background color of the item based on its column and value.

        Args:
            painter (QtGui.QPainter): The painter used for painting.
            option (QtWidgets.QStyleOptionViewItem): The style option for the item.
            index (QtCore.QModelIndex): The model index of the item.
        """
        background_color = QtGui.QColor(255, 255, 255, 0)

        state_column_index = index.sibling(index.row(), 2) # Index for column to check text
        original_text = str(index.data())  # Text of cell to be painted
        value_check = str(state_column_index.data()).upper()

        if original_text != '' and float(original_text) > float(value_check):
            background_color = QtGui.QColor(255, 0, 0) #Red

        painter.fillRect(option.rect, background_color)
        option.displayAlignment = QtCore.Qt.AlignmentFlag.AlignCenter

        super().paint(painter, option, index)

class CustomTableWidget(QtWidgets.QTableWidget):
    """
    Custom QTableWidget that supports filtering and sorting features.

    Attributes:
        list_filters (list): Stores filters applied to the table.
        column_filters (dict): Maps column indices to sets of applied filters.
        column_actions (dict): Maps column indices to actions related to columns.
        checkbox_states (dict): Stores the state of checkboxes for filtering.
        rows_hidden (dict): Maps column indices to sets of hidden row indices.
        general_rows_to_hide (set): Set of row indices that are hidden across the table.
    """
    def __init__(self, parent=None):
        """
        Initializes the CustomTableWidget.

        Sets up the initial state of the widget, including filters, checkbox states, 
        and hidden rows.

        Args:
            parent (QWidget, optional): The parent widget of this table. Defaults to None.
        """
        super().__init__(parent)
        self.list_filters=[]
        self.column_filters = {}
        self.column_actions = {}
        self.checkbox_states = {}
        self.rows_hidden = {}
        self.general_rows_to_hide = set()

# Function to show the menu
    def show_unique_values_menu(self, column_index, header_pos, header_height):
        """
        Displays a context menu for unique values in a specified column.

        The menu includes options to remove filters, sort the column, and filter by text. 
        It also allows the user to select/unselect unique values via checkboxes.

        Args:
            column_index (int): The index of the column for which the menu is displayed.
            header_pos (QPoint): The position of the header in the viewport.
            header_height (int): The height of the header.
        """
        menu = QtWidgets.QMenu(self)
        actionDeleteFilterColumn = QtGui.QAction("Quitar Filtro")
        actionDeleteFilterColumn.triggered.connect(lambda: self.delete_filter(column_index))
        menu.addAction(actionDeleteFilterColumn)
        menu.addSeparator()
        actionOrderAsc = menu.addAction("Ordenar Ascendente")
        actionOrderAsc.triggered.connect(lambda: self.sort_column(column_index, QtCore.Qt.SortOrder.AscendingOrder))
        actionOrderDesc = menu.addAction("Ordenar Descendente")
        actionOrderDesc.triggered.connect(lambda: self.sort_column(column_index, QtCore.Qt.SortOrder.DescendingOrder))
        menu.addSeparator()
        actionFilterByText = menu.addAction("Buscar Texto")
        actionFilterByText.triggered.connect(lambda: self.filter_by_text(column_index))
        menu.addSeparator()

        menu.setStyleSheet("QMenu { color: black; }"
                        "QMenu::item:selected { background-color: #33bdef; }"
                        "QMenu::item:pressed { background-color: rgb(1, 140, 190); }")

        if column_index not in self.column_filters:
            self.column_filters[column_index] = set()

        scroll_menu = QtWidgets.QScrollArea()
        scroll_menu.setWidgetResizable(True)
        scroll_widget = QtWidgets.QWidget(scroll_menu)
        scroll_menu.setWidget(scroll_widget)
        scroll_layout = QtWidgets.QVBoxLayout(scroll_widget)

        checkboxes = []

        select_all_checkbox = QtWidgets.QCheckBox("Seleccionar todo")
        if column_index in self.checkbox_states:
            select_all_checkbox.setCheckState(QtCore.Qt.CheckState(self.checkbox_states[column_index].get("Seleccionar todo", QtCore.Qt.CheckState(2))))
        else:
            select_all_checkbox.setCheckState(QtCore.Qt.CheckState(2))
        scroll_layout.addWidget(select_all_checkbox)
        checkboxes.append(select_all_checkbox)

        unique_values = self.get_unique_values(column_index)
        filtered_values = self.get_filtered_values()

        for value in sorted(unique_values):
            checkbox = QtWidgets.QCheckBox(value)
            if select_all_checkbox.isChecked(): 
                checkbox.setCheckState(QtCore.Qt.CheckState(2))
            else:
                if column_index in self.checkbox_states and value in self.checkbox_states[column_index]:
                    checkbox.setCheckState(QtCore.Qt.CheckState(self.checkbox_states[column_index][value]))
                elif filtered_values is None or value in filtered_values[column_index]:
                    checkbox.setCheckState(QtCore.Qt.CheckState(2))
                else:
                    checkbox.setCheckState(QtCore.Qt.CheckState(0))
            scroll_layout.addWidget(checkbox)
            checkboxes.append(checkbox)

        select_all_checkbox.stateChanged.connect(lambda state: self.set_all_checkboxes_state(checkboxes, state, column_index))

        for value, checkbox in zip(sorted(unique_values), checkboxes[1:]):
            checkbox.stateChanged.connect(lambda checked, value=value, checkbox=checkbox: self.apply_filter(column_index, value, checked))

    # Action for drop down menu and adding scroll area as widget
        action_scroll_menu = QtWidgets.QWidgetAction(menu)
        action_scroll_menu.setDefaultWidget(scroll_menu)
        menu.addAction(action_scroll_menu)

        menu.exec(header_pos - QtCore.QPoint(0, header_height))

# Function to delete filter on selected column
    def delete_filter(self,column_index):
        """
        Removes the filter applied to the specified column.

        Unhides previously hidden rows and resets the checkbox state for the column.

        Args:
            column_index (int): The index of the column from which to delete the filter.
        """
        if column_index in self.column_filters:
            del self.column_filters[column_index]
        if column_index in self.checkbox_states:
            del self.checkbox_states[column_index]
        if column_index in self.rows_hidden:
            for item in self.rows_hidden[column_index]:
                self.setRowHidden(item, False)
                if item in self.general_rows_to_hide:
                    self.general_rows_to_hide.remove(item)
            del self.rows_hidden[column_index]
        header_item = self.horizontalHeaderItem(column_index)
        header_item.setIcon(QtGui.QIcon())

# Function to set all checkboxes state
    def set_all_checkboxes_state(self, checkboxes, state, column_index):
        """
        Sets the state of all checkboxes in the filter menu for a specific column.

        Args:
            checkboxes (list): List of checkboxes to update.
            state (Qt.CheckState): The desired state for the checkboxes.
            column_index (int): The index of the column for which the checkboxes are set.
        """
        if column_index not in self.checkbox_states:
            self.checkbox_states[column_index] = {}

        for checkbox in checkboxes:
            checkbox.setCheckState(QtCore.Qt.CheckState(state))

        self.checkbox_states[column_index]["Seleccionar todo"] = state

# Function to apply filters to table
    def apply_filter(self, column_index, value, checked, text_filter=None, filter_dialog=None):
        """
        Applies a filter to the specified column based on the checkbox state and optional text filter.

        Args:
            column_index (int): The index of the column to filter.
            value (str): The value to filter by.
            checked (bool): Indicates if the filter should be applied (True) or removed (False).
            text_filter (str, optional): Additional text filter for filtering items. Defaults to None.
            filter_dialog (QDialog, optional): The dialog used for the text filter. Defaults to None.
        """
        if column_index not in self.column_filters:
            self.column_filters[column_index] = set()

        if text_filter is None:
            if value is None:
                self.column_filters[column_index] = set()
            elif checked:
                self.column_filters[column_index].add(value)
            elif value in self.column_filters[column_index]:
                self.column_filters[column_index].remove(value)

        rows_to_hide = set()
        for row in range(self.rowCount()):
            show_row = True

            # Check filters for all columns
            for col, filters in self.column_filters.items():
                item = self.item(row, col)
                if item:
                    item_value = item.text()
                    if text_filter is None:
                        if filters and item_value not in filters:
                            show_row = False
                            break

        # Filtering by text
            if text_filter is not None:
                filter_dialog.accept()
                item = self.item(row, column_index)
                if item:
                    if text_filter.upper() in item.text().upper():
                        self.column_filters[column_index].add(item.text())
                    else:
                        show_row = False

            if not show_row:
                if row not in self.general_rows_to_hide:
                    self.general_rows_to_hide.add(row)
                    rows_to_hide.add(row)
            else:
                if row in self.general_rows_to_hide:
                    self.general_rows_to_hide.remove(row)

        # Update hidden rows for this column depending on checkboxes
        if checked and text_filter is None:
            if column_index not in self.rows_hidden:
                self.rows_hidden[column_index] = set(rows_to_hide)
            else:
                self.rows_hidden[column_index].update(rows_to_hide)

        # Update hidden rows for this column depending on filtered text
        if text_filter is not None and value is None:
            if column_index not in self.rows_hidden:
                self.rows_hidden[column_index] = set(rows_to_hide)
            else:
                self.rows_hidden[column_index].update(rows_to_hide)

        # Iterate over all rows to hide them as necessary
        for row in range(self.rowCount()):
            self.setRowHidden(row, row in self.general_rows_to_hide)

        header_item = self.horizontalHeaderItem(column_index)
        if len(self.general_rows_to_hide) > 0:
            header_item.setIcon(QtGui.QIcon(os.path.abspath(os.path.join(basedir, "Resources/Iconos/Filter_Active.png"))))
        else:
            header_item.setIcon(QtGui.QIcon())

# Function to apply filters to table based on a desired text
    def filter_by_text(self, column_index):
        """
        Opens a dialog for filtering the specified column by text input.

        Args:
            column_index (int): The index of the column to filter.
        """
        filter_dialog = QtWidgets.QDialog(self)
        filter_dialog.setWindowTitle("Filtrar por texto")
        
        label = QtWidgets.QLabel("Texto a filtrar:")
        text_input = QtWidgets.QLineEdit()
        
        filter_button = QtWidgets.QPushButton("Filtrar")
        filter_button.setStyleSheet("QPushButton {\n"
"background-color: #33bdef;\n"
"  border: 1px solid transparent;\n"
"  border-radius: 3px;\n"
"  color: #fff;\n"
"  font-family: -apple-system,system-ui,\"Segoe UI\",\"Liberation Sans\",sans-serif;\n"
"  font-size: 15px;\n"
"  font-weight: 800;\n"
"  line-height: 1.15385;\n"
"  margin: 0;\n"
"  outline: none;\n"
"  padding: 2px .8em;\n"
"  text-align: center;\n"
"  text-decoration: none;\n"
"  vertical-align: baseline;\n"
"  white-space: nowrap;\n"
"}\n"
"\n"
"QPushButton:hover {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255);\n"
"}")
        filter_button.clicked.connect(lambda: self.apply_filter(column_index, None, False, text_input.text(), filter_dialog))

        layout = QtWidgets.QVBoxLayout()
        layout.addWidget(label)
        layout.addWidget(text_input)
        layout.addWidget(filter_button)

        filter_dialog.setLayout(layout)
        filter_dialog.exec()

# Function to obtain the unique matching applied filters 
    def get_unique_values(self, column_index):
        """
        Retrieves unique values from the specified column, taking into account any active filters on other columns.

        Args:
            column_index (int): The index of the column from which to retrieve unique values.

        Returns:
            set: A set of unique values from the specified column that are visible based on the current filters.
        """
        unique_values = set()
        for row in range(self.rowCount()):
            show_row = True
            for col, filters in self.column_filters.items():
                if col != column_index:
                    item = self.item(row, col)
                    if item:
                        item_value = item.text()
                        if filters and item_value not in filters:
                            show_row = False
                            break
            if show_row:
                item = self.item(row, column_index)
                if item:
                    unique_values.add(item.text())
        return unique_values

# Function to get values filtered by all columns
    def get_filtered_values(self):
        """
        Gets the current filter values for all columns.

        Returns:
            dict: A dictionary where each key is a column index and the value is a set of filters applied to that column.
        """
        filtered_values = {}
        for col, filters in self.column_filters.items():
            filtered_values[col] = filters
        return filtered_values

# Function to sort column
    def sort_column(self, column_index, sortOrder):
        """
        Sorts the specified column based on the given order. If the column is a date column, a custom sort method is used.

        Args:
            column_index (int): The index of the column to sort.
            sortOrder (Qt.SortOrder): The order to sort the column (ascending or descending).
        """
        if column_index == 3:
            self.custom_sort(column_index, sortOrder)
        else:
            self.sortByColumn(column_index, sortOrder)

# Function to sort column based on special datatypes
    def custom_sort(self, column, order):
        """
        Custom sorting method for date columns. Sorts the specified column based on date values.

        Args:
            column (int): The index of the column to sort.
            order (Qt.SortOrder): The order to sort the column (ascending or descending).
        """
        row_count = self.rowCount()

        indexes = list(range(row_count))
        indexes.sort(key=lambda i: QtCore.QDateTime.fromString(self.item(i, column).text(), "dd/MM/yyyy"))

        if order == QtCore.Qt.SortOrder.DescendingOrder:
            indexes.reverse()

        hidden_rows = [row for row in range(row_count) if self.isRowHidden(row)]

        rows = self.rowCount()
        for i in range(rows):
            self.insertRow(i)

        for new_row, old_row in enumerate(indexes):
            for col in range(self.columnCount()):
                item = self.takeItem(old_row + rows, col)
                self.setItem(new_row, col, item)

        for i in range(rows):
            self.removeRow(rows)

        for row in hidden_rows:
            self.setRowHidden(row, True)

# Function with the menu configuration
    def contextMenuEvent(self, event):
        """
        Handles the context menu event for the table. Shows a menu for filtering unique values when the header is right-clicked.

        Args:
            event (QEvent): The event triggered by the context menu action.
        """
        if self.horizontalHeader().visualIndexAt(event.pos().x()) >= 0:
            logical_index = self.horizontalHeader().logicalIndexAt(event.pos().x())
            header_pos = self.mapToGlobal(self.horizontalHeader().pos())
            header_height = self.horizontalHeader().height()
            self.show_unique_values_menu(logical_index, header_pos, header_height)
        else:
            super().contextMenuEvent(event)

class Ui_Humidity_Window(QtWidgets.QMainWindow):
    """
    UI class for the Client Resume window.
    """
    def __init__(self, username):
        """
        Initializes the Ui_Humidity_Window.
        """
        super().__init__()
        self.username = username
        self.setupUi(self)

    def setupUi(self, Humidity_Window):
        """
        Sets up the user interface for the Humidity_Window.

        Args:
            Humidity_Window (QtWidgets.QMainWindow): The main window for the UI setup.
        """
        Humidity_Window.setObjectName("Humidity_Window")
        icon = QtGui.QIcon()
        icon.addPixmap(QtGui.QPixmap(os.path.abspath(os.path.join(basedir, "Resources/Iconos/icon.ico"))), QtGui.QIcon.Mode.Normal, QtGui.QIcon.State.Off)
        Humidity_Window.setWindowIcon(icon)
        Humidity_Window.setStyleSheet(
    "QPushButton {\n"
    "background-color: #33bdef;\n"
    "  border: 1px solid transparent;\n"
    "  border-radius: 10px;\n"
    "  color: #fff;\n"
    "  font-family: -apple-system,system-ui,\"Segoe UI\",\"Liberation Sans\",sans-serif;\n"
    "  font-size: 15px;\n"
    "  font-weight: 800;\n"
    "  line-height: 1.15385;\n"
    "  margin: 0;\n"
    "  outline: none;\n"
    "  padding: 2px .2em;\n"
    "  text-align: center;\n"
    "  text-decoration: none;\n"
    "  vertical-align: baseline;\n"
    "  white-space: nowrap;\n"
    "}\n"
    "\n"
    "QPushButton:hover {\n"
    "    background-color: #019ad2;\n"
    "    border-color: rgb(0, 0, 0);\n"
    "}\n"
    "\n"
    "QPushButton:pressed {\n"
    "    background-color: rgb(1, 140, 190);\n"
    "    border-color: rgb(255, 255, 255);\n"
    "}"
    )
        self.centralwidget = QtWidgets.QWidget(parent=Humidity_Window)
        self.centralwidget.setObjectName("centralwidget")
        self.gridLayout = QtWidgets.QGridLayout(self.centralwidget)
        self.gridLayout.setObjectName("gridLayout")
        self.frame = QtWidgets.QFrame(parent=self.centralwidget)
        # self.frame.setMinimumSize(QtCore.QSize(1145, 860))
        self.frame.setFrameShape(QtWidgets.QFrame.Shape.StyledPanel)
        self.frame.setFrameShadow(QtWidgets.QFrame.Shadow.Raised)
        self.frame.setObjectName("frame")
        self.gridLayout_Frame = QtWidgets.QGridLayout(self.frame)
        self.gridLayout_Frame.setObjectName("gridLayout_Frame")
        spacerItem1 = QtWidgets.QSpacerItem(20, 10, QtWidgets.QSizePolicy.Policy.Minimum, QtWidgets.QSizePolicy.Policy.Fixed)
        self.gridLayout_Frame.addItem(spacerItem1, 0, 0, 1, 1)
        self.Button_Registers = QtWidgets.QPushButton(parent=self.frame)
        self.Button_Registers.setMinimumSize(QtCore.QSize(200, 50))
        font = QtGui.QFont()
        font.setPointSize(12)
        font.setBold(True)
        self.Button_Registers.setFont(font)
        self.Button_Registers.setCursor(QtGui.QCursor(QtCore.Qt.CursorShape.PointingHandCursor))
        self.Button_Registers.setObjectName("Button_Registers")
        self.Button_Registers.setText("Ver Registros")
        self.gridLayout_Frame.addWidget(self.Button_Registers, 1, 0, 1, 1)
        self.Button_Import = QtWidgets.QPushButton(parent=self.frame)
        self.Button_Import.setMinimumSize(QtCore.QSize(200, 50))
        font = QtGui.QFont()
        font.setPointSize(12)
        font.setBold(True)
        self.Button_Import.setFont(font)
        self.Button_Import.setCursor(QtGui.QCursor(QtCore.Qt.CursorShape.PointingHandCursor))
        self.Button_Import.setObjectName("Button_Import")
        self.Button_Import.setText("Importar")
        self.gridLayout_Frame.addWidget(self.Button_Import, 1, 1, 1, 1)
        spacerItem3 = QtWidgets.QSpacerItem(20, 30, QtWidgets.QSizePolicy.Policy.Minimum, QtWidgets.QSizePolicy.Policy.Fixed)
        self.gridLayout_Frame.addItem(spacerItem3, 2, 0, 1, 1)
        self.widgetspecial = QtWidgets.QWidget(parent=self.frame)
        self.widgetspecial.setObjectName("widgetspecial")
        self.gridLayout_Frame.addWidget(self.widgetspecial, 2, 0, 1, 1)
        self.gridLayout.addWidget(self.frame, 0, 0, 1, 1)
        Humidity_Window.setCentralWidget(self.centralwidget)
        self.menubar = QtWidgets.QMenuBar(parent=Humidity_Window)
        self.menubar.setGeometry(QtCore.QRect(0, 0, 1165, 22))
        self.menubar.setObjectName("menubar")
        Humidity_Window.setMenuBar(self.menubar)
        self.statusbar = QtWidgets.QStatusBar(parent=Humidity_Window)
        self.statusbar.setObjectName("statusbar")
        Humidity_Window.setStatusBar(self.statusbar)
        self.gridLayout2 = None

        self.retranslateUi(Humidity_Window)
        QtCore.QMetaObject.connectSlotsByName(Humidity_Window)

        self.Button_Import.clicked.connect(self.insert_data)

        self.load_graph()


# Function to translate and updates the text of various UI elements
    def retranslateUi(self, Humidity_Window):
        """
        Translates and updates the text of various UI elements.
        """
        _translate = QtCore.QCoreApplication.translate
        Humidity_Window.setWindowTitle(_translate("Humidity_Window", "Registro Humedad y Temperatura"))

# Function to load data from the database and create the graph
    def load_graph(self):
        """
        Loads and displays a chart to visualize the data evolution.

        Raises:
            QMessageBox: Shows error messages in case of database errors or invalid inputs.
        """

        widget_to_delete = self.frame.findChild(QtWidgets.QWidget, "widgetspecial")
        if widget_to_delete is not None:
            widget_to_delete.deleteLater()

        self.remove_layout(self.gridLayout2)

        widget_to_delete = self.frame.findChild(QtWidgets.QWidget, "widget_2")
        if widget_to_delete is not None:
            widget_to_delete.deleteLater()

        if hasattr(self, 'canvas1'):
            self.canvas1.setParent(None)
            self.canvas1.deleteLater()

        query_humidity_data = ("""
                        SELECT temperature, humidity, humidity_limit, complete_date
                        FROM verification.humidity_temp_closet
                        WHERE complete_date >= CURRENT_DATE - INTERVAL '3 months'
                        GROUP BY temperature, humidity, humidity_limit, complete_date
                        ORDER BY complete_date ASC
                        """)
        conn = None
        try:
        # read the connection parameters
            params = config()
        # connect to the PostgreSQL server
            conn = psycopg2.connect(**params)
            cur = conn.cursor()
        # execution of commands
            cur.execute(query_humidity_data)
            results=cur.fetchall()

            temp=[float(x[0]) for x in results]
            humidity=[float(x[1]) for x in results]
            humidity_limit=[float(x[2]) for x in results]
            time_stamp=[x[3] for x in results]

        except (Exception, psycopg2.DatabaseError) as error:
            dlg = QtWidgets.QMessageBox()
            new_icon = QtGui.QIcon()
            new_icon.addPixmap(QtGui.QPixmap(os.path.abspath(os.path.join(basedir, "Resources/Iconos/icon.ico"))), QtGui.QIcon.Mode.Normal, QtGui.QIcon.State.Off)
            dlg.setWindowIcon(new_icon)
            dlg.setWindowTitle("ERP EIPSA")
            dlg.setText("Ha ocurrido el siguiente error:\n"
                        + str(error))
            dlg.setIcon(QtWidgets.QMessageBox.Icon.Critical)
            dlg.exec()
            del dlg, new_icon
        finally:
            if conn is not None:
                conn.close()

        palette = QtWidgets.QApplication.palette()
        text_color = palette.color(QtGui.QPalette.ColorRole.WindowText).name()
        
        self.canvas1=FigureCanvas(Figure())
        self.canvas1.setStyleSheet("background: transparent;")
        ax=self.canvas1.figure.subplots()
        self.canvas1.figure.patch.set_alpha(0)
        ax.set_xlabel("Tiempo")
        ax.set_ylabel("Temperatura", color="tab:red")
        ax.set_facecolor("none")
        line1, = ax.plot(time_stamp, temp, label="Temperatura", color="tab:red")
        ax.tick_params(axis="y", labelcolor="tab:red")

        # Creating second Y axis sharing same X axis
        ax2 = ax.twinx()
        ax2.set_ylabel("Humedad", color="tab:blue")
        line2, = ax2.plot(time_stamp, humidity, label="Humedad", color="tab:blue")

        ax2.fill_between(time_stamp, humidity_limit, humidity, where=([h > lim for h, lim in zip(humidity, humidity_limit)]), 
                        color="red", alpha=0.3, label="Exceso")  # Highlight when it exceeds the limit
        ax2.plot(time_stamp, humidity_limit, label="Límite", color="orange", linestyle="--")

        ax2.tick_params(axis="y", labelcolor="tab:blue")
        ax2.set_facecolor("none")

        ax.set_xlabel("Fecha")
        ax.tick_params(axis="x")

        ax.legend([line1, line2], ["Temperatura", "Humedad"], loc="best", facecolor="none")
        ax.xaxis.set_major_formatter(mdates.DateFormatter('%d/%m/%Y'))

        ax.set_xlabel("Fecha", color=text_color)
        ax.tick_params(axis="x", colors=text_color)
        ax.spines["top"].set_color(text_color)
        ax.spines["bottom"].set_color(text_color)
        ax.spines["left"].set_color(text_color)
        ax.spines["right"].set_color(text_color)
        ax2.spines["top"].set_color(text_color)
        ax2.spines["bottom"].set_color(text_color)
        ax2.spines["left"].set_color(text_color)
        ax2.spines["right"].set_color(text_color)
        ax.set_title("Evolución Temperatura/Humedad", color=text_color)
        ax.legend([line1, line2], ["Temperatura", "Humedad"], loc="best", facecolor="none", edgecolor=text_color, labelcolor=text_color)

        self.canvas1.setObjectName("Graph1")
        self.gridLayout_Frame.addWidget(self.canvas1, 2, 0, 1, 2)

        self.Button_Registers.clicked.connect(self.load_table)
        self.Button_Registers.setText('Ver Tabla')

# Function to load data from the database and set the table
    def load_table(self):
        """
        Loads and displays a table to visualize the data evolution.

        Raises:
            QMessageBox: Shows error messages in case of database errors or invalid inputs.
        """

        widget_to_delete = self.frame.findChild(QtWidgets.QWidget, "widgetspecial")
        if widget_to_delete is not None:
            widget_to_delete.deleteLater()

        self.remove_layout(self.gridLayout2)

        widget_to_delete = self.frame.findChild(QtWidgets.QWidget, "widget_2")
        if widget_to_delete is not None:
            widget_to_delete.deleteLater()

        # if hasattr(self, 'canvas1'):
        #     self.canvas1.setParent(None)
        #     self.canvas1.deleteLater()

        query_humidity_data = ("""
                        SELECT temperature, humidity, humidity_limit, TO_CHAR(date_value, 'DD/MM/YYYY'), time_value, complete_date
                        FROM verification.humidity_temp_closet
                        GROUP BY temperature, humidity, humidity_limit, date_value, time_value, complete_date
                        ORDER BY complete_date DESC
                        """)
        conn = None
        try:
        # read the connection parameters
            params = config()
        # connect to the PostgreSQL server
            conn = psycopg2.connect(**params)
            cur = conn.cursor()
        # execution of commands
            cur.execute(query_humidity_data)
            results=cur.fetchall()

            column_headers = ['Temp. (°C)', 'Hum. (%)', 'Hum. Limit (%)', 'Fecha', 'Hora']

        # close communication with the PostgreSQL database server
            cur.close()
        # commit the changes
            conn.commit()

            self.tableRegisters = CustomTableWidget()
            self.tableRegisters.setMinimumSize(QtCore.QSize(650, 280))
            self.tableRegisters.setObjectName("tableRegisters")
            self.tableRegisters.setRowCount(0)
            self.tableRegisters.setRowCount(len(results))
            self.tableRegisters.setColumnCount(5)
            tablerow=0

        # fill the Qt Table with the query results
            for row in results:
                for column in range(5):
                    value = row[column]
                    if value is None:
                        value = ''
                    it = QtWidgets.QTableWidgetItem(str(value))
                    it.setFlags(it.flags() & ~QtCore.Qt.ItemFlag.ItemIsEditable)
                    self.tableRegisters.setItem(tablerow, column, it)

                tablerow+=1

            self.tableRegisters.verticalHeader().hide()
            self.tableRegisters.setItemDelegate(AlignDelegate(self.tableRegisters))
            self.color_delegate = ColorDelegate(self)
            self.tableRegisters.setItemDelegateForColumn(1, self.color_delegate)
            self.tableRegisters.setSortingEnabled(False)
            self.tableRegisters.setHorizontalHeaderLabels(column_headers)
            self.tableRegisters.horizontalHeader().setSectionResizeMode(QtWidgets.QHeaderView.ResizeMode.Stretch)
            self.tableRegisters.horizontalHeader().sectionClicked.connect(self.on_header_section_clicked)

            self.tableRegisters.horizontalHeader().setStyleSheet("QHeaderView::section {background-color: #33bdef; border: 1px solid black; font-weight: bold; font-size: 10pt;}")

            self.tableRegisters.setStyleSheet("gridline-color: rgb(128, 128, 128);")
            self.tableRegisters.horizontalHeader().setStyleSheet("QHeaderView::section {background-color: #33bdef; border: 1px solid white; font-weight: bold; font-size: 10pt;}")

        except (Exception, psycopg2.DatabaseError) as error:
            dlg = QtWidgets.QMessageBox()
            new_icon = QtGui.QIcon()
            new_icon.addPixmap(QtGui.QPixmap(os.path.abspath(os.path.join(basedir, "Resources/Iconos/icon.ico"))), QtGui.QIcon.Mode.Normal, QtGui.QIcon.State.Off)
            dlg.setWindowIcon(new_icon)
            dlg.setWindowTitle("ERP EIPSA")
            dlg.setText("Ha ocurrido el siguiente error:\n"
                        + str(error))
            dlg.setIcon(QtWidgets.QMessageBox.Icon.Critical)
            dlg.exec()
            del dlg, new_icon
        finally:
            if conn is not None:
                conn.close()

        self.gridLayout_Frame.addWidget(self.tableRegisters, 2, 0, 1, 2)

        self.Button_Registers.clicked.connect(self.load_graph)
        self.Button_Registers.setText('Ver Gráfico')

#Function to remove the layout to update it when combobox changes
    def remove_layout(self, layout):
        """
        Recursively removes all widgets from a given layout.

        Args:
            layout (QtWidgets.QLayout): The layout from which to remove widgets.
    """
        if layout is not None:
            while layout.count():
                item = layout.takeAt(0)
                widget = item.widget()
                if widget is not None:
                    widget.setParent(None)
                    widget.deleteLater()
                else:
                    self.remove_layout(item.layout())

#Function for formatting the y axis of the graphs
    def format_y_ticks(self, y, pos):
        """
        Formats the y-axis ticks for display based on their magnitude.

        Args:
            y (float): The y-value to format.
            pos (int): The position of the tick (unused in the formatting).

        Returns:
            str: The formatted tick label.
        """
        if y >= 1e6:
            return '{:.0f}M'.format(y * 1e-6)
        elif y >= 1e3:
            return '{:.0f}k'.format(y * 1e-3)
        else:
            return '{:d}'.format(int(y))

# Function to insert the data into the database:
    def insert_data(self):
        """
        Inserts data into the database.
        """
        column_names = ["id", "temperature", "humidity", "DEW", "WBT", "MIX", "date_value", "time_value"]
        fname = r'\\erp-eipsa-datos\Comunes\MARIO GIL\VERIFICACION\HIGROMETRO\importacion.txt'

        df = pd.read_csv(fname, skiprows=6, sep=r'\s+', names=column_names)

        df = df[pd.to_numeric(df["id"], errors="coerce").notna()].reset_index(drop=True)
        df = df.drop(columns=["id", "DEW", "WBT", "MIX"])

        df["temperature"] = df["temperature"].str.replace(",", ".").astype(float)
        df["humidity"] = df["humidity"].str.replace(",", ".").astype(float)

        params = config()
        conn = psycopg2.connect(**params)
        cursor = conn.cursor()

        try:
            for index, row in df.iterrows():
                columns_values = [(column, row[column]) for column in df.columns if not pd.isnull(row[column])]
                columns = ', '.join([column for column, _ in columns_values])
                values = ', '.join(['NULL' if values == '' and column in ['date_value','time_value']
                                    else f"'{values}'" for column, values in columns_values])
                
                sql_insertion = f"INSERT INTO verification.humidity_temp_closet ({columns}) VALUES ({values})"
                cursor.execute(sql_insertion)

            conn.commit()
            cursor.close()

            new_fname = "//ERP-EIPSA-DATOS/Comunes/MARIO GIL/VERIFICACION/HIGROMETRO/importacion " + date.today().strftime("%Y-%m-%d") + ".txt"

            os.rename(fname, new_fname)

            self.load_graph()
        except (Exception, psycopg2.DatabaseError) as error:
            print(error)
            dlg = QtWidgets.QMessageBox()
            new_icon = QtGui.QIcon()
            new_icon.addPixmap(QtGui.QPixmap(os.path.abspath(os.path.join(basedir, "Resources/Iconos/icon.ico"))), QtGui.QIcon.Mode.Normal, QtGui.QIcon.State.Off)
            dlg.setWindowIcon(new_icon)
            dlg.setWindowTitle("ERP EIPSA")
            dlg.setText(f"Error con la importación")
            dlg.setIcon(QtWidgets.QMessageBox.Icon.Critical)
            dlg.exec()
            del dlg, new_icon
        finally:
            if conn is not None:
                conn.close()

# Function when header of different tables is clicked
    def on_header_section_clicked(self, logical_index):
        """
        Handles the click event on the table header.
        Displays a context menu for unique values in the clicked column header.
        """
        header_pos = self.tableRegisters.horizontalHeader().sectionViewportPosition(logical_index)
        header_height = self.tableRegisters.horizontalHeader().height()
        popup_pos = self.tableRegisters.viewport().mapToGlobal(QtCore.QPoint(header_pos, header_height))
        self.tableRegisters.show_unique_values_menu(logical_index, popup_pos, header_height)



if __name__ == "__main__":
    import sys
    app = QtWidgets.QApplication(sys.argv)
    Humidity_Window = QtWidgets.QMainWindow()
    ui = Ui_Humidity_Window('m.gil')
    ui.setupUi(Humidity_Window)
    Humidity_Window.show()
    sys.exit(app.exec())
