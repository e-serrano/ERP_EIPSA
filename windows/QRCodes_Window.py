# Form implementation generated from reading ui file 'QRCodes_Window.ui'
#
# Created by: PySide6 UI code generator 6.4.1
#
# WARNING: Any manual changes made to this file will be lost when pyuic6 is
# run again.  Do not edit this file unless you know what you are doing.

import re
from PySide6 import QtCore, QtGui, QtWidgets
import psycopg2
from config.config_functions import config_database, get_path
import locale
import pandas as pd
import os
from datetime import *
from openpyxl.styles import NamedStyle
from utils.Database_Manager import Database_Connection
from utils.Show_Message import MessageHelper
from decimal import Decimal

basedir = r"\\ERP-EIPSA-DATOS\Comunes\EIPSA-ERP"


class AlignDelegate(QtWidgets.QStyledItemDelegate):
    """
    A custom item delegate for aligning cell content in a QTableView or QTableWidget to the center.

    Inherits from:
        QtWidgets.QStyledItemDelegate: Provides custom rendering and editing for table items.

    """
    def initStyleOption(self, option, index):
        """
        Initializes the style option for the item, setting its display alignment to center.

        Args:
            option (QtWidgets.QStyleOptionViewItem): The style option to initialize.
            index (QtCore.QModelIndex): The model index of the item.
        """
        super(AlignDelegate, self).initStyleOption(option, index)
        option.displayAlignment = QtCore.Qt.AlignmentFlag.AlignCenter

class CustomTableWidget(QtWidgets.QTableWidget):
    """
    Custom QTableWidget that supports filtering and sorting features.

    Attributes:
        list_filters (list): Stores filters applied to the table.
        column_filters (dict): Maps column indices to sets of applied filters.
        column_actions (dict): Maps column indices to actions related to columns.
        checkbox_states (dict): Stores the state of checkboxes for filtering.
        rows_hidden (dict): Maps column indices to sets of hidden row indices.
        general_rows_to_hide (set): Set of row indices that are hidden across the table.
    """
    def __init__(self, parent=None):
        """
        Initializes the CustomTableWidget.

        Sets up the initial state of the widget, including filters, checkbox states, 
        and hidden rows.

        Args:
            parent (QWidget, optional): The parent widget of this table. Defaults to None.
        """
        super().__init__(parent)
        self.list_filters=[]
        self.column_filters = {}
        self.column_actions = {}
        self.checkbox_states = {}
        self.rows_hidden = {}
        self.general_rows_to_hide = set()

# Function to show the menu
    def show_unique_values_menu(self, column_index, header_pos, header_height):
        """
        Displays a context menu for unique values in a specified column.

        The menu includes options to remove filters, sort the column, and filter by text. 
        It also allows the user to select/unselect unique values via checkboxes.

        Args:
            column_index (int): The index of the column for which the menu is displayed.
            header_pos (QPoint): The position of the header in the viewport.
            header_height (int): The height of the header.
        """
        menu = QtWidgets.QMenu(self)
        actionDeleteFilterColumn = QtGui.QAction("Quitar Filtro")
        actionDeleteFilterColumn.triggered.connect(lambda: self.delete_filter(column_index))
        menu.addAction(actionDeleteFilterColumn)
        menu.addSeparator()
        actionOrderAsc = menu.addAction("Ordenar Ascendente")
        actionOrderAsc.triggered.connect(lambda: self.sort_column(column_index, QtCore.Qt.SortOrder.AscendingOrder))
        actionOrderDesc = menu.addAction("Ordenar Descendente")
        actionOrderDesc.triggered.connect(lambda: self.sort_column(column_index, QtCore.Qt.SortOrder.DescendingOrder))
        menu.addSeparator()
        actionFilterByText = menu.addAction("Buscar Texto")
        actionFilterByText.triggered.connect(lambda: self.filter_by_text(column_index))
        menu.addSeparator()

        menu.setStyleSheet("QMenu { color: black; }"
                        "QMenu::item:selected { background-color: #33bdef; }"
                        "QMenu::item:pressed { background-color: rgb(1, 140, 190); }")

        if column_index not in self.column_filters:
            self.column_filters[column_index] = set()

        scroll_menu = QtWidgets.QScrollArea()
        scroll_menu.setWidgetResizable(True)
        scroll_widget = QtWidgets.QWidget(scroll_menu)
        scroll_menu.setWidget(scroll_widget)
        scroll_layout = QtWidgets.QVBoxLayout(scroll_widget)

        checkboxes = []

        select_all_checkbox = QtWidgets.QCheckBox("Seleccionar todo")
        if column_index in self.checkbox_states:
            select_all_checkbox.setCheckState(QtCore.Qt.CheckState(self.checkbox_states[column_index].get("Seleccionar todo", QtCore.Qt.CheckState(2))))
        else:
            select_all_checkbox.setCheckState(QtCore.Qt.CheckState(2))
        scroll_layout.addWidget(select_all_checkbox)
        checkboxes.append(select_all_checkbox)

        unique_values = self.get_unique_values(column_index)
        filtered_values = self.get_filtered_values()

        for value in sorted(unique_values):
            checkbox = QtWidgets.QCheckBox(value)
            if select_all_checkbox.isChecked(): 
                checkbox.setCheckState(QtCore.Qt.CheckState(2))
            else:
                if column_index in self.checkbox_states and value in self.checkbox_states[column_index]:
                    checkbox.setCheckState(QtCore.Qt.CheckState(self.checkbox_states[column_index][value]))
                elif filtered_values is None or value in filtered_values[column_index]:
                    checkbox.setCheckState(QtCore.Qt.CheckState(2))
                else:
                    checkbox.setCheckState(QtCore.Qt.CheckState(0))
            scroll_layout.addWidget(checkbox)
            checkboxes.append(checkbox)

        select_all_checkbox.stateChanged.connect(lambda state: self.set_all_checkboxes_state(checkboxes, state, column_index))

        for value, checkbox in zip(sorted(unique_values), checkboxes[1:]):
            checkbox.stateChanged.connect(lambda checked, value=value, checkbox=checkbox: self.apply_filter(column_index, value, checked))

    # Action for drop down menu and adding scroll area as widget
        action_scroll_menu = QtWidgets.QWidgetAction(menu)
        action_scroll_menu.setDefaultWidget(scroll_menu)
        menu.addAction(action_scroll_menu)

        menu.exec(header_pos - QtCore.QPoint(0, header_height))


# Function to delete filter on selected column
    def delete_filter(self,column_index):
        """
        Removes the filter applied to the specified column.

        Unhides previously hidden rows and resets the checkbox state for the column.

        Args:
            column_index (int): The index of the column from which to delete the filter.
        """
        if column_index in self.column_filters:
            del self.column_filters[column_index]
        if column_index in self.checkbox_states:
            del self.checkbox_states[column_index]
        if column_index in self.rows_hidden:
            for item in self.rows_hidden[column_index]:
                self.setRowHidden(item, False)
                if item in self.general_rows_to_hide:
                    self.general_rows_to_hide.remove(item)
            del self.rows_hidden[column_index]
        header_item = self.horizontalHeaderItem(column_index)
        header_item.setIcon(QtGui.QIcon())


# Function to set all checkboxes state
    def set_all_checkboxes_state(self, checkboxes, state, column_index):
        """
        Sets the state of all checkboxes in the filter menu for a specific column.

        Args:
            checkboxes (list): List of checkboxes to update.
            state (Qt.CheckState): The desired state for the checkboxes.
            column_index (int): The index of the column for which the checkboxes are set.
        """
        if column_index not in self.checkbox_states:
            self.checkbox_states[column_index] = {}

        for checkbox in checkboxes:
            checkbox.setCheckState(QtCore.Qt.CheckState(state))

        self.checkbox_states[column_index]["Seleccionar todo"] = state


# Function to apply filters to table
    def apply_filter(self, column_index, value, checked, text_filter=None, filter_dialog=None):
        """
        Applies a filter to the specified column based on the checkbox state and optional text filter.

        Args:
            column_index (int): The index of the column to filter.
            value (str): The value to filter by.
            checked (bool): Indicates if the filter should be applied (True) or removed (False).
            text_filter (str, optional): Additional text filter for filtering items. Defaults to None.
            filter_dialog (QDialog, optional): The dialog used for the text filter. Defaults to None.
        """
        if column_index not in self.column_filters:
            self.column_filters[column_index] = set()

        if text_filter is None:
            if value is None:
                self.column_filters[column_index] = set()
            elif checked:
                self.column_filters[column_index].add(value)
            elif value in self.column_filters[column_index]:
                self.column_filters[column_index].remove(value)

        rows_to_hide = set()
        for row in range(self.rowCount()):
            show_row = True

            # Check filters for all columns
            for col, filters in self.column_filters.items():
                item = self.item(row, col)
                if item:
                    item_value = item.text()
                    if text_filter is None:
                        if filters and item_value not in filters:
                            show_row = False
                            break

        # Filtering by text
            if text_filter is not None:
                filter_dialog.accept()
                item = self.item(row, column_index)
                if item:
                    if text_filter.upper() in item.text().upper():
                        self.column_filters[column_index].add(item.text())
                    else:
                        show_row = False

            if not show_row:
                if row not in self.general_rows_to_hide:
                    self.general_rows_to_hide.add(row)
                    rows_to_hide.add(row)
            else:
                if row in self.general_rows_to_hide:
                    self.general_rows_to_hide.remove(row)

        # Update hidden rows for this column depending on checkboxes
        if checked and text_filter is None:
            if column_index not in self.rows_hidden:
                self.rows_hidden[column_index] = set(rows_to_hide)
            else:
                self.rows_hidden[column_index].update(rows_to_hide)

        # Update hidden rows for this column depending on filtered text
        if text_filter is not None and value is None:
            if column_index not in self.rows_hidden:
                self.rows_hidden[column_index] = set(rows_to_hide)
            else:
                self.rows_hidden[column_index].update(rows_to_hide)

        # Iterate over all rows to hide them as necessary
        for row in range(self.rowCount()):
            hidden = False

            for col, filters in self.column_filters.items():
                if filters:
                    item = self.item(row, col)
                    item_value = item.text() if item else ""
                    if item_value not in filters:
                        hidden = True
                        break

            self.setRowHidden(row, hidden)

        header_item = self.horizontalHeaderItem(column_index)
        if len(self.general_rows_to_hide) > 0:
            header_item.setIcon(QtGui.QIcon(os.path.abspath(os.path.join(basedir, "Resources/Iconos/Filter_Active.png"))))
        else:
            header_item.setIcon(QtGui.QIcon())

# Function to apply filters to table based on a desired text
    def filter_by_text(self, column_index):
        """
        Opens a dialog for filtering the specified column by text input.

        Args:
            column_index (int): The index of the column to filter.
        """
        filter_dialog = QtWidgets.QDialog(self)
        filter_dialog.setWindowTitle("Filtrar por texto")
        
        label = QtWidgets.QLabel("Texto a filtrar:")
        text_input = QtWidgets.QLineEdit()
        
        filter_button = QtWidgets.QPushButton("Filtrar")
        filter_button.setStyleSheet("QPushButton {\n"
"background-color: #33bdef;\n"
"  border: 1px solid transparent;\n"
"  border-radius: 3px;\n"
"  color: #fff;\n"
"  font-family: -apple-system,system-ui,\"Segoe UI\",\"Liberation Sans\",sans-serif;\n"
"  font-size: 15px;\n"
"  font-weight: 800;\n"
"  line-height: 1.15385;\n"
"  margin: 0;\n"
"  outline: none;\n"
"  padding: 2px .8em;\n"
"  text-align: center;\n"
"  text-decoration: none;\n"
"  vertical-align: baseline;\n"
"  white-space: nowrap;\n"
"}\n"
"\n"
"QPushButton:hover {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255);\n"
"}")
        filter_button.clicked.connect(lambda: self.apply_filter(column_index, None, False, text_input.text(), filter_dialog))

        layout = QtWidgets.QVBoxLayout()
        layout.addWidget(label)
        layout.addWidget(text_input)
        layout.addWidget(filter_button)

        filter_dialog.setLayout(layout)
        filter_dialog.exec()


# Function to obtain the unique matching applied filters 
    def get_unique_values(self, column_index):
        """
        Retrieves unique values from the specified column, taking into account any active filters on other columns.

        Args:
            column_index (int): The index of the column from which to retrieve unique values.

        Returns:
            set: A set of unique values from the specified column that are visible based on the current filters.
        """
        unique_values = set()
        for row in range(self.rowCount()):
            show_row = True
            for col, filters in self.column_filters.items():
                if col != column_index:
                    item = self.item(row, col)
                    if item:
                        item_value = item.text()
                        if filters and item_value not in filters:
                            show_row = False
                            break
            if show_row:
                item = self.item(row, column_index)
                if item:
                    unique_values.add(item.text())
        return unique_values

# Function to get values filtered by all columns
    def get_filtered_values(self):
        """
        Gets the current filter values for all columns.

        Returns:
            dict: A dictionary where each key is a column index and the value is a set of filters applied to that column.
        """
        filtered_values = {}
        for col, filters in self.column_filters.items():
            filtered_values[col] = filters
        return filtered_values

# Function to sort column
    def sort_column(self, column_index, sortOrder):
        """
        Sorts the specified column based on the given order. If the column is a date column, a custom sort method is used.

        Args:
            column_index (int): The index of the column to sort.
            sortOrder (Qt.SortOrder): The order to sort the column (ascending or descending).
        """
        if column_index in [9, 13, 15, 16]:
            self.custom_sort(column_index, sortOrder)
        else:
            self.sortByColumn(column_index, sortOrder)


    def custom_sort(self, column, order):
        """
        Custom sorting method for date and money columns. Sorts the specified column based on date and money values.

        Args:
            column (int): The index of the column to sort.
            order (Qt.SortOrder): The order to sort the column (ascending or descending).
        """
        if column in [13, 15, 16]:
            row_count = self.rowCount()

            indexes = list(range(row_count))
            indexes.sort(key=lambda i: QtCore.QDateTime.fromString(self.item(i, column).text(), "dd/MM/yyyy"))

            if order == QtCore.Qt.SortOrder.DescendingOrder:
                indexes.reverse()

            hidden_rows = [row for row in range(row_count) if self.isRowHidden(row)]

            rows = self.rowCount()
            for i in range(rows):
                self.insertRow(i)

            for new_row, old_row in enumerate(indexes):
                for col in range(self.columnCount()):
                    item = self.takeItem(old_row + rows, col)
                    self.setItem(new_row, col, item)

            for i in range(rows):
                self.removeRow(rows)

            for row in hidden_rows:
                self.setRowHidden(row, True)

        elif column in [9]:
            row_count = self.rowCount()

            indexes = list(range(row_count))
            indexes.sort(key=lambda i: float(self.item(i, column).text().replace(" €","").replace(".", "").replace(",", ".")) if self.item(i, column).text() else float('inf'))

            if order == QtCore.Qt.SortOrder.DescendingOrder:
                indexes.reverse()

            hidden_rows = [row for row in range(row_count) if self.isRowHidden(row)]

            rows = self.rowCount()
            for i in range(rows):
                self.insertRow(i)

            for new_row, old_row in enumerate(indexes):
                for col in range(self.columnCount()):
                    item = self.takeItem(old_row + rows, col)
                    self.setItem(new_row, col, item)

            for i in range(rows):
                self.removeRow(rows)

            for row in hidden_rows:
                self.setRowHidden(row, True)

# Function with the menu configuration
    def contextMenuEvent(self, event):
        """
        Handles the context menu event for the table. Shows a menu for filtering unique values when the header is right-clicked.

        Args:
            event (QEvent): The event triggered by the context menu action.
        """
        if self.horizontalHeader().visualIndexAt(event.pos().x()) >= 0:
            logical_index = self.horizontalHeader().logicalIndexAt(event.pos().x())
            header_pos = self.mapToGlobal(self.horizontalHeader().pos())
            header_height = self.horizontalHeader().height()
            self.show_unique_values_menu(logical_index, header_pos, header_height)
        else:
            super().contextMenuEvent(event)


class Ui_QRCodes_Window(QtWidgets.QMainWindow):
    """
    UI class for the Query Offer window.
    """
    def __init__(self, username):
        """
        Initializes the Ui_QRCodes_Window.
        """
        super().__init__()
        self.username = username
        self.reference = None
        self.setupUi(self)

    def setupUi(self, QRCodes_Window):
        """
        Sets up the user interface for the QRCodes_Window.

        Args:
            QRCodes_Window (QtWidgets.QMainWindow): The main window for the UI setup.
        """
        QRCodes_Window.setObjectName("QRCodes_Window")
        QRCodes_Window.resize(845, 590)
        QRCodes_Window.setMinimumSize(QtCore.QSize(1000, 590))
        icon = QtGui.QIcon()
        icon.addPixmap(QtGui.QPixmap(os.path.abspath(os.path.join(basedir, "Resources/Iconos/icon.ico"))), QtGui.QIcon.Mode.Normal, QtGui.QIcon.State.Off)
        QRCodes_Window.setWindowIcon(icon)
        QRCodes_Window.setStyleSheet(
"QPushButton {\n"
"background-color: #33bdef;\n"
"  border: 1px solid transparent;\n"
"  border-radius: 3px;\n"
"  color: #fff;\n"
"  font-family: -apple-system,system-ui,\"Segoe UI\",\"Liberation Sans\",sans-serif;\n"
"  font-size: 22px;\n"
"  font-weight: 800;\n"
"  line-height: 1.15385;\n"
"  margin: 0;\n"
"  outline: none;\n"
"  padding: 4px .4em;\n"
"  text-align: center;\n"
"  text-decoration: none;\n"
"  vertical-align: baseline;\n"
"  white-space: nowrap;\n"
"}\n"
"\n"
"QPushButton:hover {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:focus {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255)\n"
"}\n"
"\n"
"QPushButton:focus:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255);\n"
"}")
        self.centralwidget = QtWidgets.QWidget(parent=QRCodes_Window)
        self.centralwidget.setObjectName("centralwidget")
        self.gridLayout = QtWidgets.QGridLayout(self.centralwidget)
        self.gridLayout.setObjectName("gridLayout")
        self.frame = QtWidgets.QFrame(parent=self.centralwidget)
        self.frame.setFrameShape(QtWidgets.QFrame.Shape.StyledPanel)
        self.frame.setFrameShadow(QtWidgets.QFrame.Shadow.Raised)
        self.frame.setObjectName("frame")
        self.gridLayout_2 = QtWidgets.QGridLayout(self.frame)
        self.gridLayout_2.setVerticalSpacing(10)
        self.gridLayout_2.setObjectName("gridLayout_2")
        self.label_Scan= QtWidgets.QLabel(parent=self.frame)
        self.label_Scan.setMinimumSize(QtCore.QSize(175, 25))
        # self.label_Scan.setMaximumSize(QtCore.QSize(90, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Scan.setFont(font)
        self.label_Scan.setObjectName("label_Scan")
        self.label_Scan.setText("Código:")
        self.gridLayout_2.addWidget(self.label_Scan, 0, 0, 1, 2)
        self.Scan_Code = QtWidgets.QLineEdit(parent=self.frame)
        self.Scan_Code.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Scan_Code.setFont(font)
        self.Scan_Code.setObjectName("Scan_Code")
        self.Scan_Code.setStyleSheet("""
QLineEdit {
    border: 2px solid palette(highlight);
    background-color: rgba(128, 128, 128, 60);
    color: palette(text);
}
""")
        self.gridLayout_2.addWidget(self.Scan_Code, 0, 2, 1, 2)
        self.Button_Clear = QtWidgets.QPushButton(parent=self.frame)
        self.Button_Clear.setMinimumSize(QtCore.QSize(150, 35))
        self.Button_Clear.setMaximumSize(QtCore.QSize(150, 35))
        self.Button_Clear.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        self.Button_Clear.setStyleSheet("QPushButton {\n"
"background-color: #33bdef;\n"
"  border: 1px solid transparent;\n"
"  border-radius: 3px;\n"
"  color: #fff;\n"
"  font-family: -apple-system,system-ui,\"Segoe UI\",\"Liberation Sans\",sans-serif;\n"
"  font-size: 22px;\n"
"  font-weight: 800;\n"
"  line-height: 1.15385;\n"
"  margin: 0;\n"
"  outline: none;\n"
"  padding: 4px .4em;\n"
"  text-align: center;\n"
"  text-decoration: none;\n"
"  vertical-align: baseline;\n"
"  white-space: nowrap;\n"
"}\n"
"\n"
"QPushButton:hover {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:focus {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255)\n"
"}\n"
"\n"
"QPushButton:focus:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255);\n"
"}")
        self.Button_Clear.setObjectName("Button_Clear")
        self.Button_Clear.setText('Limpiar')
        self.gridLayout_2.addWidget(self.Button_Clear, 1, 0, 1, 2)
        self.Button_In = QtWidgets.QPushButton(parent=self.frame)
        self.Button_In.setMinimumSize(QtCore.QSize(100, 35))
        self.Button_In.setMaximumSize(QtCore.QSize(100, 35))
        self.Button_In.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        self.Button_In.setStyleSheet("QPushButton {\n"
"background-color: #33bdef;\n"
"  border: 1px solid transparent;\n"
"  border-radius: 3px;\n"
"  color: #fff;\n"
"  font-family: -apple-system,system-ui,\"Segoe UI\",\"Liberation Sans\",sans-serif;\n"
"  font-size: 22px;\n"
"  font-weight: 800;\n"
"  line-height: 1.15385;\n"
"  margin: 0;\n"
"  outline: none;\n"
"  padding: 2px .2em;\n"
"  text-align: center;\n"
"  text-decoration: none;\n"
"  vertical-align: baseline;\n"
"  white-space: nowrap;\n"
"}\n"
"\n"
"QPushButton:hover {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:focus {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255)\n"
"}\n"
"\n"
"QPushButton:focus:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255);\n"
"}")
        self.Button_In.setObjectName("Button_In")
        self.Button_In.setText('Entrada')
        self.gridLayout_2.addWidget(self.Button_In, 1, 2, 1, 1)
        self.Button_Out = QtWidgets.QPushButton(parent=self.frame)
        self.Button_Out.setMinimumSize(QtCore.QSize(100, 35))
        self.Button_Out.setMaximumSize(QtCore.QSize(100, 35))
        self.Button_Out.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        self.Button_Out.setStyleSheet("QPushButton {\n"
"background-color: #33bdef;\n"
"  border: 1px solid transparent;\n"
"  border-radius: 3px;\n"
"  color: #fff;\n"
"  font-family: -apple-system,system-ui,\"Segoe UI\",\"Liberation Sans\",sans-serif;\n"
"  font-size: 22px;\n"
"  font-weight: 800;\n"
"  line-height: 1.15385;\n"
"  margin: 0;\n"
"  outline: none;\n"
"  padding: 2px .2em;\n"
"  text-align: center;\n"
"  text-decoration: none;\n"
"  vertical-align: baseline;\n"
"  white-space: nowrap;\n"
"}\n"
"\n"
"QPushButton:hover {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:focus {\n"
"    background-color: #019ad2;\n"
"    border-color: rgb(0, 0, 0);\n"
"}\n"
"\n"
"QPushButton:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255)\n"
"}\n"
"\n"
"QPushButton:focus:pressed {\n"
"    background-color: rgb(1, 140, 190);\n"
"    border-color: rgb(255, 255, 255);\n"
"}")
        self.Button_Out.setObjectName("Button_Out")
        self.Button_Out.setText('Salida')
        self.gridLayout_2.addWidget(self.Button_Out, 1, 3, 1, 1)
        spacerItem = QtWidgets.QSpacerItem(20, 10, QtWidgets.QSizePolicy.Policy.Minimum, QtWidgets.QSizePolicy.Policy.Fixed)
        self.gridLayout_2.addItem(spacerItem, 2, 0, 1, 1)
        self.label_Reference= QtWidgets.QLabel(parent=self.frame)
        self.label_Reference.setMinimumSize(QtCore.QSize(175, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Reference.setFont(font)
        self.label_Reference.setObjectName("label_Reference")
        self.label_Reference.setText("Referencia:")
        self.gridLayout_2.addWidget(self.label_Reference, 3, 0, 1, 2)
        self.Reference_Text = QtWidgets.QLabel(parent=self.frame)
        self.Reference_Text.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Reference_Text.setFont(font)
        self.Reference_Text.setObjectName("Reference_Text")
        self.Reference_Text.setStyleSheet("""QLabel {border: 2px solid #444;}""")
        self.gridLayout_2.addWidget(self.Reference_Text, 3, 2, 1, 2)
        self.label_Description= QtWidgets.QLabel(parent=self.frame)
        self.label_Description.setMinimumSize(QtCore.QSize(175, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Description.setFont(font)
        self.label_Description.setObjectName("label_Description")
        self.label_Description.setText("Descripción:")
        self.gridLayout_2.addWidget(self.label_Description, 4, 0, 1, 2)
        self.Description_Text = QtWidgets.QLabel(parent=self.frame)
        self.Description_Text.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Description_Text.setFont(font)
        self.Description_Text.setObjectName("Description_Text")
        self.Description_Text.setStyleSheet("""QLabel {border: 2px solid #444;}""")
        self.gridLayout_2.addWidget(self.Description_Text, 4, 2, 1, 2)
        self.label_Physical= QtWidgets.QLabel(parent=self.frame)
        self.label_Physical.setMinimumSize(QtCore.QSize(175, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Physical.setFont(font)
        self.label_Physical.setObjectName("label_Physical")
        self.label_Physical.setText("St. Físico:")
        self.gridLayout_2.addWidget(self.label_Physical, 5, 0, 1, 2)
        self.Physical_Text = QtWidgets.QLabel(parent=self.frame)
        self.Physical_Text.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Physical_Text.setFont(font)
        self.Physical_Text.setObjectName("Physical_Text")
        self.Physical_Text.setStyleSheet("""QLabel {border: 2px solid #444;}""")
        self.gridLayout_2.addWidget(self.Physical_Text, 5, 2, 1, 2)
        self.label_Available= QtWidgets.QLabel(parent=self.frame)
        self.label_Available.setMinimumSize(QtCore.QSize(175, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Available.setFont(font)
        self.label_Available.setObjectName("label_Available")
        self.label_Available.setText("St. Disp.:")
        self.gridLayout_2.addWidget(self.label_Available, 6, 0, 1, 2)
        self.Available_Text = QtWidgets.QLabel(parent=self.frame)
        self.Available_Text.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Available_Text.setFont(font)
        self.Available_Text.setObjectName("Available_Text")
        self.Available_Text.setStyleSheet("""QLabel {border: 2px solid #444;}""")
        self.gridLayout_2.addWidget(self.Available_Text, 6, 2, 1, 2)
        self.label_Pending= QtWidgets.QLabel(parent=self.frame)
        self.label_Pending.setMinimumSize(QtCore.QSize(175, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Pending.setFont(font)
        self.label_Pending.setObjectName("label_Pending")
        self.label_Pending.setText("Pendiente:")
        self.gridLayout_2.addWidget(self.label_Pending, 7, 0, 1, 2)
        self.Pending_Text = QtWidgets.QLabel(parent=self.frame)
        self.Pending_Text.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Pending_Text.setFont(font)
        self.Pending_Text.setObjectName("Pending_Text")
        self.Pending_Text.setStyleSheet("""QLabel {border: 2px solid #444;}""")
        self.gridLayout_2.addWidget(self.Pending_Text, 7, 2, 1, 2)
        self.label_Virtual= QtWidgets.QLabel(parent=self.frame)
        self.label_Virtual.setMinimumSize(QtCore.QSize(175, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Virtual.setFont(font)
        self.label_Virtual.setObjectName("label_Virtual")
        self.label_Virtual.setText("St. Virtual:")
        self.gridLayout_2.addWidget(self.label_Virtual, 8, 0, 1, 2)
        self.Virtual_Text = QtWidgets.QLabel(parent=self.frame)
        self.Virtual_Text.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Virtual_Text.setFont(font)
        self.Virtual_Text.setObjectName("Virtual_Text")
        self.Virtual_Text.setStyleSheet("""QLabel {border: 2px solid #444;}""")
        self.gridLayout_2.addWidget(self.Virtual_Text, 8, 2, 1, 2)
        self.label_Location= QtWidgets.QLabel(parent=self.frame)
        self.label_Location.setMinimumSize(QtCore.QSize(175, 25))
        font = QtGui.QFont()
        font.setPointSize(22)
        font.setBold(True)
        self.label_Location.setFont(font)
        self.label_Location.setObjectName("label_Location")
        self.label_Location.setText("Ubicación:")
        self.gridLayout_2.addWidget(self.label_Location, 9, 0, 1, 2)
        self.Location_Text = QtWidgets.QLabel(parent=self.frame)
        self.Location_Text.setMinimumSize(QtCore.QSize(450, 50))
        font = QtGui.QFont()
        font.setPointSize(22)
        self.Location_Text.setFont(font)
        self.Location_Text.setObjectName("Location_Text")
        self.Location_Text.setStyleSheet("""QLabel {border: 2px solid #444;}""")
        self.gridLayout_2.addWidget(self.Location_Text, 9, 2, 1, 2)

        self.gridLayout.addWidget(self.frame, 0, 0, 1, 1)
        QRCodes_Window.setCentralWidget(self.centralwidget)
        self.menubar = QtWidgets.QMenuBar(parent=QRCodes_Window)
        self.menubar.setGeometry(QtCore.QRect(0, 0, 845, 22))
        self.menubar.setObjectName("menubar")
        QRCodes_Window.setMenuBar(self.menubar)
        self.statusbar = QtWidgets.QStatusBar(parent=QRCodes_Window)
        self.statusbar.setObjectName("statusbar")
        QRCodes_Window.setStatusBar(self.statusbar)

        self.retranslateUi(QRCodes_Window)
        QtCore.QMetaObject.connectSlotsByName(QRCodes_Window)
        self.Scan_Code.returnPressed.connect(self.query_filtered)
        self.Button_Clear.clicked.connect(self.clear_input)
        self.Button_In.clicked.connect(self.reference_in)
        self.Button_Out.clicked.connect(self.reference_out)

        self.Scan_Code.setFocus()

# Function to translate and updates the text of various UI elements
    def retranslateUi(self, QRCodes_Window):
        """
        Translates and updates the text of various UI elements.
        """
        _translate = QtCore.QCoreApplication.translate
        QRCodes_Window.setWindowTitle(_translate("QRCodes_Window", "Inventario"))

    def query_filtered(self):
        """
        Queries the database for filtered offers, configures and populates tables with the query results, 
        and updates the UI accordingly. Handles potential database errors and updates the UI with appropriate messages.
        """
        self.reference = self.Scan_Code.text().upper()

        commands_query = ("""
                    SELECT reference, description, physical_stock, available_stock, pending_stock, virtual_stock, location
                    FROM purch_fact.supplies
                    WHERE reference = %s 
                    """)

        try:
            with Database_Connection(config_database()) as conn:
                with conn.cursor() as cur:
                    cur.execute(commands_query, (self.reference,))
                    results=cur.fetchall()

                    if results:
                        self.Reference_Text.setText(results[0][0])
                        self.Description_Text.setText(results[0][1])
                        self.Physical_Text.setText(str(results[0][2]))
                        self.Available_Text.setText(str(results[0][3]))
                        self.Pending_Text.setText(str(results[0][4]))
                        self.Virtual_Text.setText(str(results[0][5]))
                        self.Location_Text.setText(results[0][6])

                        # self.Scan_Code.clear()
                    else:
                        self.Reference_Text.clear()
                        self.Description_Text.clear()
                        self.Physical_Text.clear()
                        self.Available_Text.clear()
                        self.Pending_Text.clear()
                        self.Virtual_Text.clear()
                        self.Location_Text.clear()

        except (Exception, psycopg2.DatabaseError) as error:
            MessageHelper.show_message("Ha ocurrido el siguiente error:\n"
                        + str(error), "critical")

    def clear_input(self):
        self.Reference_Code.setText("")
        self.reference = None

    def reference_in(self):
        if self.reference is not None:
            dlg = QtWidgets.QInputDialog()
            new_icon = QtGui.QIcon()
            new_icon.addPixmap(QtGui.QPixmap(str(get_path("Resources", "Iconos", "icon.ico"))), QtGui.QIcon.Mode.Normal, QtGui.QIcon.State.Off)
            dlg.setWindowIcon(new_icon)
            dlg.setWindowTitle('Entrada Material')
            dlg.setLabelText('Cantidad:')
            font = QtGui.QFont()
            font.setPointSize(22)
            dlg.setFont(font)

            while True:
                clickedButton = dlg.exec()
                if clickedButton == 1:
                    quantity = dlg.textValue()
                    if quantity != '':
                        # MessageHelper.show_message(f"Referencia: {self.reference}\nSe suma cantidad ({quantity}) al stock físico y se resta del pendiente", "info")
                        query_material_in = ("UPDATE purch_fact.supplies SET physical_stock = physical_stock + %s, pending_stock = pending_stock - %s WHERE reference = %s")
                        with Database_Connection(config_database()) as conn:
                            with conn.cursor() as cur:
                                cur.execute(query_material_in, (Decimal(quantity), Decimal(quantity), self.reference,))
                            conn.commit()
                        break   
                    MessageHelper.show_message("La cantidad no puede estar vacía", "warning")
                else:
                    break

            self.query_filtered()

    def reference_out(self):
        if self.reference is not None:
            dlg = QtWidgets.QInputDialog()
            new_icon = QtGui.QIcon()
            new_icon.addPixmap(QtGui.QPixmap(str(get_path("Resources", "Iconos", "icon.ico"))), QtGui.QIcon.Mode.Normal, QtGui.QIcon.State.Off)
            dlg.setWindowIcon(new_icon)
            dlg.setWindowTitle('Salida Material')
            dlg.setLabelText('Cantidad:')
            font = QtGui.QFont()
            font.setPointSize(22)
            dlg.setFont(font)

            while True:
                clickedButton = dlg.exec()
                if clickedButton == 1:
                    quantity = dlg.textValue()
                    if quantity != '':
                        # MessageHelper.show_message(f"Referencia: {self.reference}\nSe resta cantidad ({quantity}) del stock físico", "info")
                        query_material_out = ("UPDATE purch_fact.supplies SET physical_stock = physical_stock - %s WHERE reference = %s")
                        with Database_Connection(config_database()) as conn:
                            with conn.cursor() as cur:
                                cur.execute(query_material_out, (Decimal(quantity), self.reference,))
                            conn.commit()
                        break   
                    MessageHelper.show_message("La cantidad no puede estar vacía", "warning")
                else:
                    break

            self.query_filtered()


if __name__ == "__main__":
    import sys
    import os

    ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
    if ROOT not in sys.path:
        sys.path.insert(0, ROOT)
    app = QtWidgets.QApplication(sys.argv)
    QRCodes_Window = Ui_QRCodes_Window('d.marquez')
    QRCodes_Window.showMaximized()
    sys.exit(app.exec())
